# Task PRD-0502: Dockerfile and compose profiles

> PRD: PRD-05 | Depends: PRD-0501
> Files: `Dockerfile.serve`, `docker-compose.serve.yml`, `.env.serve.example` | Lines: ~120 | Time: ~60min

## What to Build
Implement dockerfile and compose profiles for PRD-05, keeping interfaces aligned with the LSFDNet paper and the reference implementation.

## Files
- `Dockerfile.serve`, `docker-compose.serve.yml`, `.env.serve.example`

## Verify

env PYTHONPATH=src test -f Dockerfile.serve && test -f docker-compose.serve.yml

## Done When
- [ ] File(s) created and imports work
- [ ] Local check command passes
- [ ] Shapes and behavior are consistent with PRD acceptance criteria
